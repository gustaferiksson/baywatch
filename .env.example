# Anthropic API key used by sandcastle's claudeCode agent inside the sandbox container.
ANTHROPIC_API_KEY=

# GitHub auth: baywatch shells out to `gh`, so just `gh auth login` once.
# No GITHUB_TOKEN needed here unless you want to override.
